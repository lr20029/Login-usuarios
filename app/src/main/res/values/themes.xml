<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base para modo claro -->
    <style name="Base.Theme.Login" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blue</item>
        <item name="android:textColorPrimary">@color/grey</item>
        <item name="android:textViewStyle">@style/textviewPersonalizado</item>
        <item name="editTextStyle">@style/edittextPersonalizado</item>
    </style>

    <!-- Estilo para TextView -->
    <style name="textviewPersonalizado" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">15dp</item>
        <item name="android:fontFamily">sans-serif-black</item>
        <item name="android:fontStyle">italic</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Estilo para EditText -->
    <style name="edittextPersonalizado" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/texto_principal_oscuro</item>
        <item name="android:textSize">15dp</item>
        <item name="android:background">@color/fondo_input_azul</item>
        <item name="android:textColorHint">@color/texto_hint_gris</item>
    </style>

    <!-- Tema principal -->
    <style name="Theme.Login" parent="Base.Theme.Login" />
</resources>